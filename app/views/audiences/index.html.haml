- unless @audiences.nil?
  %table#audiences
    %tbody
      = render :partial => 'audience', :collection => @audiences
= link_to '+', new_schedule_audience_path, :id => :add_audience, :remote => true
