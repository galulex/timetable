%td{ :colspan => 2 }
  = form_for [@schedule, @object], :remote => true do |f|
    %table
      %tr
        %td= f.text_field :title, :placeholder => placeholder_for(@object, :title)
        %td.actions
          = f.submit t(:ok)
          = link_to t("cancel"), ""

