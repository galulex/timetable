%h1= t(".schedules")

- unless @schedules.nil?
  - @schedules.each do |schedule|
    = link_to schedule.title, schedule_path(schedule)
= link_to t(".create_new_schedule"), new_schedule_path, :rel => :lazybox
