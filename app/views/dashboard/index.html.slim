- unless current_user
  =' link_to t(".sign_in"), new_session_path, remote: true, class: 'button' unless params[:controller] == 'sessions'
  =' link_to t(".sign_up"), new_user_path, remote: true, class: 'button'
  = link_to t(".login_facebook"), facebook_url
