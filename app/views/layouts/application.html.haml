!!!
%html
  %head
    %title Timetable
    = stylesheet_link_tag    "application"
    = javascript_include_tag "application"
    = csrf_meta_tags
  %body
    .header
      - unless @schedule.nil?
        = link_to 'Teachers', schedule_teachers_path(@schedule), :class => 'gray button'
        = link_to 'Audiences', schedule_audiences_path(@schedule), :class => 'gray button'
        = link_to 'Groups', schedule_groups_path(@schedule), :class => 'gray button'
        = link_to 'Schedule Calls', schedule_schedule_calls_path(@schedule), :class => 'gray button'
      - if current_user
        = link_to current_user.name, edit_user_path(current_user), :rel => :lazybox, :class => 'orange button'
        = link_to "Sign Out", session_path, :method => :delete, :class => 'orange button'
    .message= flash[:notice] if flash[:notice]
    .message= flash[:error] if flash[:error]
    .container
      = yield
