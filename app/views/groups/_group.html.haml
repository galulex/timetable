%tr.group{:id => "group_#{group.id}"}
  %td= group.name
  %td= group.teacher.name
  %td
    = link_to('edit', edit_schedule_group_path(@schedule, group), :rel => :lazybox)
    = link_to('×', schedule_group_path(@schedule, group), :method => :delete, :remote => true)
