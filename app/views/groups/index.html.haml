- unless @groups.nil?
  %table#groups
    %thead
      %tr
        %th Name
        %th Teacher
        %th Actions
    %tbody
      = render :partial => 'group', :collection => @groups
    %tfoot
      %tr
        %td= link_to '+', new_schedule_group_path, :id => :add_group, :remote => true
